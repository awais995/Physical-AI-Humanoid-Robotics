// Chatbot configuration script
// This script is loaded by Docusaurus to provide configuration to the chatbot
// Default API URL can be overridden by setting window.chatbotConfig.apiBaseUrl before this script loads
window.chatbotConfig = {
  apiBaseUrl: window.chatbotConfig?.apiBaseUrl || 'https://awaissoomro-chat-bot.hf.space'
};