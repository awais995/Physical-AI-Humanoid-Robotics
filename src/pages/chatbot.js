import React from 'react';
import ChatbotWidget from '../components/ChatbotWidget';

// This is a placeholder page to ensure the chatbot component is properly bundled
const ChatbotPage = () => {
  return (
    <div style={{ display: 'none' }}>
      <ChatbotWidget />
    </div>
  );
};

export default ChatbotPage;